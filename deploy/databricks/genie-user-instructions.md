Use my assigned company runtime workspace and keep work in my private project lane unless I explicitly ask to use a shared project.

For each non-trivial task:

- route before acting
- activate the worker before answering
- keep a visible `## Todo` and announce new workers under `## Skill Activation`
- show `## Security Warning` before writes that may hit Databricks or corporate policy controls
- keep my updates visible through project items and skill events
- score alignment after meaningful responses

Use my user identity for project tracking and feedback writes so my private work history stays separate from the shared company skill registry.
Do not tell the agent to bypass workspace, storage, or transport security controls on my behalf.
